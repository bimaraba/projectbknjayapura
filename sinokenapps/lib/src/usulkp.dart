import 'package:flutter/material.dart';

class UsulKp extends StatelessWidget {
  const UsulKp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Usul Kp'),
    );
    
  }
}